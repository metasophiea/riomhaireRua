// example
~
command:data
command:data
command:data

routine:routName
    command:data
    command:data
    command:data
    end:routName

~
command:data
command:data
command:data
run:routName
command:data
command:data
command:data



 - in conversion -  
routine:routName      ->      essentially produces a goto command that points to the routine's associated 'end' command
end                   ->      commandNumber:routineNumber   -> activates the machine's "get top address from stack" function
runRoutine:routName   ->      commandNumber:routineNumber   -> activates the machine's "place address on stack" function, and goes to the first line of the routine
so essentially, the converter changes the calling of a routine, from just being the routine's name to a 'run routine' command followed by the routine's number


